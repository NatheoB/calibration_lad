---
title: "LAD calibration"
output: html_document
date: "2025-09-24"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
	echo = FALSE,
	message = FALSE,
	warning = FALSE
)
```

```{r}
library(targets)
library(dplyr)
library(ggplot2)
```


# Observe residuals for each simulation

```{r}
tar_load(out_residuals)

out_residuals %>% 
  ggplot(aes(y = res_pacl, x = lad, color = replicate)) +
  facet_wrap(~site) +
  geom_point()
```

